BOT_TOKEN=your_token_here
LOG_LEVEL=INFO

# Only needed for local polling mode (not used on PythonAnywhere)
POLLING_TIMEOUT=30

# Required on PythonAnywhere free tier (outbound connections go through proxy)
# Leave empty for local development
PROXY_URL=
